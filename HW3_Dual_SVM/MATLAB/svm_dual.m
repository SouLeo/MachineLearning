function [support_vecs, count] = svm_dual(train_img_col, svm_labels, max_iter)
    [n_features, n_samples]= size(train_img_col);
    alphas = zeros(n_samples);
    for i = 1:max_iter
        
    end
end